export const TIMELINE_QUERY = `*[_type == "entry"] | order(date asc) {
  _id,
  title,
  date,
  excerpt,
  "slug": slug.current,
  tags,
  rooms,
  media[]{
    _type,
    _key,
    asset->{
      _id,
      url,
      originalFilename,
      mimeType,
      metadata,
      playbackId,
      assetId,
      status
    },
    thumbnail{
      asset->{
        _id,
        url
      }
    }
  },
  relatedDocs[]->{
    _id,
    title,
    slug
  }
}`;
